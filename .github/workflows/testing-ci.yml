name: Node.JS CI of testing server

on:
  pull_request:
    branches: [ testing ]

jobs:
  build:

    runs-on: [self-hosted, testing, api, ci]

    steps:
    - uses: actions/checkout@v2
    - run: npm ci
    - run: npm run build